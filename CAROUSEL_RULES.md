# IG Carousel 內容與版型規則 v2

> 2026-07-26 改版。舊版（九天磨一個品牌、每張獨立生成 4:5）已作廢。
> 依據見 `CAROUSEL_RESEARCH.md`；架構理由見 `CAROUSEL_V2_DESIGN.md`。

## 產品定義

- **一天 = 一個可販售產品**，不是一天一張圖。母圖 → 分割 → Canva 上字，當天做完。
- **14 天一循環**：先 9 天 **行業包**（一天一個買家導向行業），再 5 天 **百搭 Kit**（5 套結構型）。
  行業包排前面，因為教練／房仲／攝影師／課程創作者才是真的會掏錢的人（`CAROUSEL_RESEARCH.md` §4）。

## 不能長得都一樣（2026-07-26 加）

每天的樣子是五個**正交維度**的組合，各自用互質的步長輪替（`pick()`）：

| 維度 | 數量 | 例 |
| --- | --- | --- |
| 視覺語法 `VISUAL_FAMILIES` | 20 | 靜謐拱窗編輯誌、瑞士方格、孔版疊印、票券票根、夜光卡… |
| 配色 `COLOURWAYS` | 10 | warm-neutral、sea-glass、plum-paper、sand-cobalt… |
| 字型配對 `TYPE_PAIRINGS` | 6 | serif-led、condensed-impact、mono-accent… |
| 表面質感 `SURFACES` | 5 | uncoated-paper、laid-textile、soft-glaze… |
| 故事結構 `STORY_STRUCTURES` | 7 | 列表／前後對比／教學步驟／金句／誤解破除／客戶疑慮／案例拆解 |

- **測試守住這件事**：`test_no_two_days_ever_look_the_same` 檢查 140 天內沒有任何兩天撞組合，
  且同一個行業每輪拿到的款式都不同。
- ⚠️ 7 種結構會被 14 天的循環整除，單用日次會讓同一行業每輪都拿到同一結構 —— 所以結構的
  index 要額外加上輪次（`day_index + round_index`）。改循環天數或結構數時要重跑這個測試。
- 頁數跟著**結構**走（金句 6、客戶疑慮 7、對比與誤解破除 8、清單／步驟／案例 9），不硬湊九張。

## 尺寸

- **3:4，1080×1440**。這是 2026 Feed 與個人頁 Grid 的比例，封面不會被裁。
- 同一組**每張必須同比例**，混用會被 Instagram 強制裁切。
- ⚠️ 輪播上傳器目前仍**預設 4:5**。交付給買家的說明書必須寫一句「上傳時改成 3:4」。

## 張數

- 允許範圍 **6–12**，由結構決定，不是隨便選：清單／步驟／案例 9、對比／誤解破除 8、客戶疑慮 7、金句 6。
- 8–10 張的互動率一致較高；互動在第 3 張後下降、第 8 張後回升。
- 要縮短時，優先刪「視覺停頓」與最後一個並列項；**封面與 CTA 永遠保留**。

## 母圖（連續背景帶）

- 母圖**只放背景**：底色、紙紋、色帶、裝飾線、留白節奏。
- **母圖絕對不含文字**。文字被分割 + 放大後必爛，一律由 Canva 上字。
- 結構元素（線、帶、路徑、撕邊、漸層）必須**從左緣連續到右緣**。
- 自成一體的圖案**不可跨在切點上**：要嘛避開切點，要嘛真的做成連續的。
- 對比要沿整條帶保持平均——一端深一端淺會讓卡片看起來不像同一組。
- 一張母圖一個配色。配色變化用另一張母圖，不是同一張裡漸變。

## 分割

- 用 `scripts/split_carousel.py`，輸出 N 張 + `_seam_preview.png`。
- **先看 seam preview 再繼續**：紅帶是每個切點左右各 80px 的禁區。
- `--fit stretch`（預設）只適用抽象紋理母圖；有拱門、照片框、物件等可辨識結構時改 `--fit cover`。

## 每張卡片

- 每張卡片只演**它自己的敘事角色**，不要每張都塞滿所有欄位。
- **Logo、CTA、Hashtag 只出現在封面與最後一張。**
- 封面必須在 160px 縮圖下可讀：一句主導文案、字級 90px 以上、對比至少 4.5:1。
- 安全邊 80px；左右邊緣再各留 80px 安靜區，因為卡片在滑動時彼此相鄰。
- 內文 36px 以上，真實可讀的台灣繁體中文。

## 內容型態（決定會不會被存檔）

演算法把**存檔**當最強訊號，所以型態要選會被存的：教學步驟、前後對比、清單框架、
**誤解破除**、FAQ 客戶疑慮。想要觸及就衝分享，想要權威就衝存檔。

## 交付給買家

- 圖層命名：`BACKGROUND / HEADLINE / BODY / LABEL / PHOTO FRAME / PAGE / LOGO / CTA`。
- 最多兩個字型家族、三個品牌色。
- ⚠️ **字型只能用可外發的授權。Canva Pro 專屬字型不能用在要交付的模板。**
- 存成 master design，每次客製前先複製，最後產出 **Brand Template 連結**交付。
