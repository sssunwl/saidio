# IG Carousel v2 設計提案｜母圖分割 + 可變張數 + 可販售流程

> 狀態：**提案，尚未實作。** 現行規則仍是 `CAROUSEL_RULES.md`（九天一品牌、每張獨立生成 4:5）。
> 這份要回答的問題：能不能「一次生成一張圖，再分割成一組圖」，讓一個品牌一天做完、同組風格必然統一。
> 撰寫日：2026-07-25。

## 1. 結論：可行，但能切的只有「沒有文字的那一層」

先算數字，因為這決定整個架構：

| 目標 | 需要的母圖寬度 | 影像模型實際能給 |
| --- | --- | --- |
| 6 張 × 1080 寬 | 6480 px | 長邊約 1536 px |
| 9 張 × 1080 寬 | 9720 px | 長邊約 1536 px |

硬生一張 1536 寬的長圖再切 6 份，每格只拿到 **256 px 原始寬度**，放大到 1080 是 4.2×。
背景紋理撐得住，**文字和人臉一定爛**。所以：

- ❌ 「AI 生一張含文字的九宮格長圖 → 切開 → 直接發」——這條路在畫質上不成立。
- ✅ 「AI 生一張**無文字的連續背景長條**（panorama plate）→ 切成 N 片 → Canva 每片上文字」。

母圖負責：底色、紙紋、色帶、裝飾線條、照片帶、留白節奏。
Canva 負責：所有文字、Logo、CTA、頁碼、可替換的照片框。

這剛好命中你要的三件事：

1. **一個品牌一天做完**——一張母圖 + 一次分割，不用排九天。
2. **同組必然統一**——它們本來就是同一張圖，不是九次「希望它像」。
3. **接駁位元素免費得到**——線條、色帶、照片帶天然跨越切點，不必刻意對齊。

於是「九天」的意義反轉：**一天一個品牌，九天 = 九個品牌**，而不是九天磨一個品牌。

### 畫質再往上推的做法（要用到付費工具時）
1. 先生一張 3:2 基準圖 → 用 outpaint 往左右延伸到需要的長寬比（本工作區已接的 Higgsfield MCP 有 `outpaint_image`）。
2. 再 `upscale_image` 到 4K，最後才切。
3. 或分 2–3 段重疊生成，用重疊區手動接。
免費路線就是：接受母圖只放背景，文字全部走 Canva。

## 2. 必須知道的前提：IG 輪播不是無縫的

滑動時 IG 會露出下一張的邊，兩張之間有間隙，**不會像全景照片一樣連續**。
所以「長圖分割」的實際效果是「滑到下一張時發現它接得上」的驚喜感，不是一張連續大圖。

設計上必須**雙軌成立**：

- 每一張單獨看要完整——多數人只看第一張。
- 連續滑要接得上——這是加分項，不是主結構。
- **接駁位左右各 80 px 內不放文字、人臉、Logo**。IG 會再壓縮一次，切邊最容易爛。

## 3. 尺寸要先定：4:5 還是 3:4

現行生成器寫 4:5（1080×1350）。但工作區自己的 2026 規格筆記寫：

- 3:4（1080×1440）＝ Feed 與個人頁 Grid **都不裁切**，是現在主推
- 4:5（1080×1350）＝ 仍可用，但 **Grid 縮圖會削掉兩側**，貼邊的 Logo/文字會被切

賣模板給別人時，「在個人頁不會被削」是很實際的賣點。
→ **建議改 3:4**，長條母圖變成 `1080N × 1440`。
生成器已把尺寸抽成 `CARD_WIDTH / CARD_HEIGHT / CARD_RATIO` 三個常數，改一行即可。
（Meta 規格每年會變，正式上架前用 WebSearch 核一次官方最新指引。）

## 4. 張數 6/7/8/9：按內容型態決定，不是隨便選

| 張數 | 適合的型態 | 特性 |
| --- | --- | --- |
| 6 | 清單型、對比型 | 完讀率最高，最好做完 |
| 7 | 教學型（1 hook + 5 步 + 1 CTA） | 步驟數剛好不累 |
| 8 | 故事型 | 有起伏空間 |
| 9 | 資料／懶人包型 | 存檔率高，完讀率最低 |

**賣模板的實務做法**：一個產品給滿 9 頁，但在說明書裡明寫「要縮成 6／7／8 頁時刪哪幾頁」。
買家最怕的就是不知道能刪哪頁——這比賣四個不同張數的版本更值錢，也少維護三份檔案。

技術上張數只是分割器的參數 `N`，母圖寬度 `1080 × N`。

## 5. 動圖／影片格

IG 輪播可以混圖片與影片。兩個放法：

- **第 1 張放 3–5 秒微動影片**——拉停留時間，演算法吃 watch time。
- **「視覺停頓」那張放動態**——不搶 hook，但讓整組不呆。

賣的時候不能直接賣 MP4 當模板。要賣的是 **Canva 動態頁**（Canva 有頁面動畫 + 匯出 MP4），
買家換掉素材自己匯出。這是可以往上標價的差異點，一般模板賣家不做。

## 6. 百搭款：存在，而且應該先做

真正百搭的是**結構**，不是行業。四款吃掉大部分需求：

1. **列表款**——「N 個重點」／「N 個常見錯誤」
2. **前後對比款**——before/after、錯 vs 對
3. **教學步驟款**——Step 1…N
4. **金句款**——一句引言 + 大字，最好做、存檔率高

**4 款 × 3 組配色 = 12 個變體**，這本身就是一個可上架的 Kit，不必等行業研究做完。
行業專屬款（咖啡／牙醫／健身）是第二批，用來吃 niche 關鍵字和拉高單價。

## 7. 行業研究要抓什麼（下一步，需要你點頭）

不是「看什麼好看」，是抓三個可驗證的東西：

1. 該行業真實 IG 帳號實際在發哪幾種輪播型態——數出現次數，不憑感覺。
2. Canva／Creative Market／Etsy 上賣得動的模板**標題關鍵字**——這是產品命名的依據。
3. 該行業的「客戶疑慮清單」——這是 FAQ 款的內容來源，也是最容易做出差異的地方。

## 8. 生成完之後，怎麼真的賣出去

（呼應 Gumroad 選品研究：賣得動的是 **Kit 打包**不是純 PDF；免費／自由定價下載量最大；產品靠內容導流。）

### 第一步：產品化——不是把圖丟上去
- 母圖切片 → 在 Canva 重建成 **Brand Template**（品牌模板連結），不是普通 design 分享連結。
- 一個產品應包含：N 頁模板 + 字體與配色說明 + **「怎麼刪頁」指南** + 3 組示範文案 + 授權說明。
- 交付物：Canva 模板連結 + PDF 使用說明 +（選配）MP4 動態版。

### 第二步：授權先想清楚——跳過這步會出事
- AI 生成圖的商用條款、字體授權（**Canva Pro 字體不能外發**）、照片來源。
- 寫一張 `LICENSE.md`：買家可用於自己或其客戶的社群貼文，**不可轉售模板本身**。

### 第三步：定價階梯
| 層級 | 內容 | 作用 |
| --- | --- | --- |
| 免費／自由定價 | 1 個百搭模板 | 導流、換 email，下載量最大的一層 |
| US$9–19 | 百搭 Kit（4 款 × 3 配色） | 主力入門品 |
| US$39–59 | 行業包（9 頁完整故事線 + 文案庫） | 主要利潤 |
| US$99+ | 代理／多客戶授權 | 少量但單價高 |

### 第四步：上架（Gumroad 為主，repo 已存在）
- 標題吃關鍵字：`Instagram Carousel Templates for [行業] — Canva, 9 pages, 繁體中文`
- **封面圖最重要**：一張九格拼貼 + 一張手機 mockup。
- 至少 3 張預覽圖 + 一支 15 秒滑動 demo 影片。

### 第五步：導流——沒有這步不會賣
- 用模板本身發自己的 IG，那就是活廣告。
- 每個模板配一支 Reels「這組我怎麼做的」，簡介放連結。
- 進 AI 學院當教材：把「怎麼用 AI 做模板」做成課，模板當附件。

### 第六步：驗證門檻——做第二個之前先過關
- 前 10 個免費下載 → 找 3 個人問「你會付多少」。
- **有人真的用它發文** → 才做第二個行業包。
- 沒有人用 → 問題在文案與需求，不在設計，別急著再畫。

## 9. 要動生成器之前，需要決定的三件事

1. 尺寸改 **3:4** 還是留 4:5？
2. 第一批做**百搭 4 款**，還是繼續九天行業包？
3. 行業研究現在跑嗎？

決定後要改的檔案：`scripts/generate_carousel_briefs.py`（母圖 prompt + 分割規格取代九張獨立 prompt）、
`CAROUSEL_RULES.md`（規則改寫）、新增分割器腳本。
